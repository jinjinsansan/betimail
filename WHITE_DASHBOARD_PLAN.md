# 白のダッシュボード（afi.irah.uk）再構築 実装計画書

**作成日**: 2026-07-20 / **仕様確定**: 2026-07-20（仁氏回答 → §7）
**ステータス**: 実装中
**前例**: v1.2.0 lucky（PROJECT_STATE §17）/ ポータル再構築（PROJECT_STATE §19、`PORTAL_REBUILD_PLAN.md`）

---

## 1. 背景と重要な違い

afi.irah.uk（通称: 白のダッシュボード。アフィリエイト系サイト）を betimail 内に自前再構築する第3の案件。

**⚠ lucky / ポータルとの決定的な違い: afi はまだ稼働中**（2026-07-20 時点で出金同期 cron が正常動作、
サイト応答あり）。「死んだサイトの復旧」ではなく**「生きているサイトの計画的な置き換え」**であり、
切り替え（カットオーバー）設計が最重要になる。

## 2. 役割（仁氏指定・2026-07-20）

- **見るサイト + 独自残高の出金**:
  1. 会員権NFT の保有枚数表示
  2. パチスロホイホイ の保有枚数表示
  3. ウォレット残高（afi 残高）の表示
  4. **afi 残高の出金申請**（ウォレットアドレス記入 → 管理画面で仁氏が処理）
- ポータル（/portal）の残高とは**別系統・干渉しない**
- 分配機能は作らない（ポータルと違い、管理画面からの原資分配は無し ※要確認: 報酬が今も発生するか → §7-3）
- アフィリエイト報酬計算（title/packet/MLM構造）の再現は**非スコープ**（残高スナップショット + 出金のみ）

## 3. 規模（2026-05-15 スクレイピング時点 — 稼働中のため変動あり）

| 項目 | 値 |
|---|---:|
| ユーザー総数 | 2,905 |
| **残高 > 0** | **618 名 / 合計 $158,997.94** |
| 会員権NFT 購入記録 | 707 行（546名） |
| デバイス（ホイホイ）購入記録 | 322 行（376名 ※list_user展開） |
| 出金履歴（同期済み） | 1,197 件（うち未承認 20件 / $6,449 was 2026-06-05） |

ポータル（$35K）より大きい金額を扱う。**残高の完全一致要件はここでも適用**（カットオーバー時点のスナップショットと一致させる）。

## 4. データ取得の2案

| 案 | 方法 | 備考 |
|---|---|---|
| **A案（推奨）** | ベトナム法人から SQL ダンプ | ポータルと同じ ETL + 会員別完全一致検証。残高の正本が明確 |
| B案 | 稼働中サイトをスクレイピング | 実績あり（`scrape_afi.py`、admin ユーザー一覧に balance が載る）。ダンプが出ない場合の確実な代替 |

いずれも**カットオーバー直前に最終取得**する（今取ってもその後に残高が動く）。

## 5. カットオーバー設計（最重要）

afi が生きたまま新サイトで出金受付を始めると、**同じ残高で両サイトから二重に出金申請できてしまう**。

```
① afi 側の出金受付を停止（またはX日で締切と告知）← ベトナム法人/K氏との調整が必要
② 最終データ取得（残高スナップショット = 正本確定）
③ ETL + 完全一致検証 + 未処理出金申請の引き継ぎ確認
④ 新サイト公開（ソフトローンチ → ゲート解除）+ 会員告知
⑤ afi 閉鎖（または閲覧のみ化）
```

②〜④の間に afi 側で残高が動くと不一致になるため、①→④は短期間（数日以内）で実施する。

## 6. 実装方針（ポータルの実装を流用 — 開発は短期で可能）

- **DB**: `afi_members`（email/name/balance/wallet）・`afi_assets`（会員権/ホイホイ枚数）・`afi_withdrawals`
  （portal_withdrawals と同型: pending→processing→paid/rejected、paid で残高減算、pending合計の超過申請ガード）。
  旧出金履歴は既存 `withdraw_requests` source='afi'（1,197件）を閲覧用に流用
- **会員UI**: `/portal` の構造・OTPログイン（scope=afi）を流用。テーマは白基調 + ポータル（紺）と別のアクセント（§7-4）
- **管理**: 既存「アフィリエイト出金」タブを拡張 or 新タブ。出金処理 + 会員検索 + Telegram 即時通知
- **AI連携**: `_build_afi_block()` を lucky/portal と並列追加
- **ソフトローンチ**: `AFI_ALLOWED_EMAILS` ゲート + preview 会員（確立済みパターン）
- 既存の afi 出金同期 cron（15,45 * * * *）はカットオーバー後に停止

## 7. 確定仕様（2026-07-20 仁氏回答）

1. **データ取得 = スクレイピングのみ**（ベトナム法人からのダンプは無し。もらった ZIP はポータル分のみ）
2. **カットオーバー調整は可能**（afi 側の出金受付停止を調整できる）
3. **残高は固定**。ビジネスモデルは停止済み = アフィリエイト報酬はもう発生しない → 凍結スナップショットで完結
4. 名称は**「白のダッシュボード」**で確定。ルートは `/white`（表示名: 白のダッシュボード）
5. **出金は受け付けるが、送金はポータル側の出金処理を優先**。
   会員UIに「出金申請は受け付けておりますが、送金はポータルサイト側の出金処理を優先して行うため、
   今しばらくお待ちください」の注意書きを常設（申請フォーム + 申請完了時の両方）
6. **公開タイミング: ポータルサイトと白のダッシュボードを同時公開**。
   → ポータルのゲート解除・告知メルマガは白のダッシュボード完成まで保留し、告知は1通で両サイトを案内する

## 8. スケジュール

実装約1週間（ポータルの流用率が高い）。完成 → 仁氏実機確認 → afi 出金受付停止 →
最終スクレイピング（残高スナップショット確定）→ ETL → 両サイト同時にゲート解除 → 告知メルマガ配信。
